<p id="notice"><%= notice %></p>

<p>
  <h2><%= @note.title %></h2>
</p>

<p>
  <%= sanitize @note.body %>
</p>

<%= link_to "Edit #{@note.title}", edit_folder_note_path(@folder, @note) %> |
<%= link_to "See all from #{@folder.name}", folder_path(@folder) %> | 
<%= link_to "All notes", notes_path %>