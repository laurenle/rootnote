<%= render 'form', note: @note, folder: @folder %>

<%= link_to "Back to #{@note.title}", folder_note_path(@folder, @note) %> |
<%= link_to "See all from #{@folder.name}", folder_path(@folder) %>
<%= link_to "All notes", notes_path %>