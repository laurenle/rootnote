<!-- Setting these variables here because this is used in so many places -->
<% @upload = Upload.new
   @uploads = current_user.uploads.order(created_at: :desc) %>

<%= render 'uploads/form' %>
<%= render 'uploads/display' %>